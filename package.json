{
  "name": "afkir-qibla-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "prebuild": "npm --prefix frontend ci || npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0",
    "web-push": "^3.6.7"
  }
}
