[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"                  # <- IKKE frontend/dist når base=frontend

[functions]
  directory = "../netlify/functions"  # <- ett nivå opp
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
