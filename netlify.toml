[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"                   # <-- relativ til base

[functions]
  directory = "../netlify/functions" # <-- ett hakk opp fra base
  node_bundler = "esbuild"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
