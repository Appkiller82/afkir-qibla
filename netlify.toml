[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Sørg for at service worker kan kontrollere hele domenet
[[headers]]
  for = "/service-worker.js"
  [headers.values]
    Service-Worker-Allowed = "/"
